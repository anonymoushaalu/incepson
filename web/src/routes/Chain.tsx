import { PagePlaceholder } from "../components/PagePlaceholder.js";

export function Chain() {
  return (
    <PagePlaceholder
      title="On-Chain"
      milestone="A6"
      description="Settled transactions, HashScan links, and balance deltas."
    />
  );
}
