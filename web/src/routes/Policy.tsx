import { PagePlaceholder } from "../components/PagePlaceholder.js";

export function Policy() {
  return (
    <PagePlaceholder
      title="Policy"
      milestone="A7"
      description="policy.json as an interactive, steppable rule chain."
    />
  );
}
