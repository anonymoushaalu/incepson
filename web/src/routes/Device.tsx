import { PagePlaceholder } from "../components/PagePlaceholder.js";

export function Device() {
  return (
    <PagePlaceholder
      title="Device"
      milestone="A8"
      description="The escalation loop made inspectable: OLED mirror, countdown, tamper/replay checks."
    />
  );
}
